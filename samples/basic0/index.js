import { SATBBook } from '../../src/satblib.js';

export class Basic0Book extends SATBBook {
    constructor() {
        super("Basic 0");

        this.addScene(
            () => {

            }
        );
    }
}