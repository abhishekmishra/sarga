import { SargaBook } from '../../src/sarga_runtime.js';

export class Basic0Book extends SargaBook {
    constructor() {
        super("Basic 0");

        this.addBlock(
            () => {

            }
        );
    }
}